#include<stdio.h>
#include<conio.h>
int main()
{
int n,sum=0,r,temp;
printf("enter number: ");
scanf("%d",&n);
temp=n;
while(n>0){
    r=n%10;
    sum=(sum*10)+r;
n=n/10;
}
if(temp==sum)
     prinf("number is palindrome");
else
     printf("number is not palindrome");
}     
